[package]
name = "proto"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
tonic = "0.12"
prost = "0.13"
prost-types = "0.13"
common = { path = "../common", default-features = false }

[build-dependencies]
tonic-build = "0.12"
